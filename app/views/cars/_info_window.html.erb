<%= link_to(car_path(car.id), class: "map-info-box", data: { turbo: false }) do %>
  <p><%= car.title %> - <%= car.address %></p>
  <%=  cl_image_tag car.photo.key, width: 200, height: 100, crop: :fill %>
<% end %>
